/_site/
/.quarto/
.DS_Store
Thumbs.db

**/*.quarto_ipynb

# Agent worktrees — these are full git checkouts, never commit them
/.claude/worktrees/

# Deck/media scratch (32MB of extracted PowerPoint assets)
/_cap/

# Regenerable draft renders — .qmd and .svg sources are tracked instead
_drafts/**/index.html
_drafts/**/*.pdf
_drafts/**/index_files/

# wrangler local dev state (bundles + local D1)
worker/.wrangler/
worker/node_modules/
